// AuthPrompt.tsx — removed in v8.0.
// This component used popupLogin() which is no longer supported.
// Use <BinrcLoginButton> or call signIn() from useAuth() instead.
export interface AuthPromptProps {}
export function AuthPrompt(_props: AuthPromptProps) { return null }
