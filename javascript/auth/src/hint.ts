// hint.ts is superseded by onetap.ts (One Tap iframe model).
// getHint() cross-origin fetch is removed in v7.4 — PII no longer crosses
// the origin boundary before user interaction.
// This file is kept as a tombstone; do not re-export from index.ts.
